# 安全策略

## 报告漏洞

**请不要用公开 Issue 报告安全问题。** 发邮件到 38004547@qq.com，标题带
`[ClawMe Security]`。收到后 72 小时内回复。

如果漏洞可能影响正在运行的部署，请在报告里说明你是否已经验证过公网可复现，
以及是否需要我们先通知已知的部署方。

## 这个项目的威胁模型

ClawMe 把指令送到你的浏览器和手机上，并能在你的电脑上执行确认过的动作。
换句话说，**攻破 relay ≈ 攻破被它连着的设备**。因此：

- **relay 不持有秘密，也不做决定。** 它路由和存事件，动作由 owner 节点执行。
  绕开手机上的按钮不等于绕开权限——确认在 owner 端强制。
- **客户端一律按可被反编译对待。** 手机 App 里没有烤死的凭据；配对令牌由用户
  输入、加密存进 Android Keystore / iOS Keychain。
- **生物识别不出设备。** 手机只上报确认时间和方式，不上传任何生物特征或可复用
  的认证秘密。
- **写动作绑定到具体动作、输入摘要和状态版本。** 一次性挑战由 owner 签发，
  relay 不能自批；状态陈旧返回冲突而不是照做。

## 部署时最容易出错的地方

### relay 必须配置凭据

relay 在没有 `CLAWME_TOKENS` 或 `CLAWME_IDENTITIES` 时**拒绝启动**（退出码 78）。

这条规则来自一次真实事故：早期版本把"没配置凭据"当成"开发模式，放行所有人"，
而 `deploy/setup.sh` 会把它挂到一条公网 Cloudflare 隧道上。结果是任何人用一个
随手编的令牌就能拿到 HTTP 200。修复见 commit `caf521b`。

如果你在本地确实想要一个谁都能进的 relay，必须显式写出来：

```bash
CLAWME_ALLOW_ANY_TOKEN=1 npm start   # 且只允许绑定回环地址
```

### 令牌就是门禁

一个 relay 令牌能向你的浏览器和手机下发指令。当成密码保管：

- `deploy/setup.sh` 会生成 32 字节随机令牌写入 `/etc/clawme/token`（权限 600）
- 不要用 `test` 之类的示例值，不要提交进版本库，不要贴进 Issue
- 怀疑泄露就删掉 `/etc/clawme/token` 重跑部署脚本，然后更新各端

### 监听地址

relay 默认只绑 `127.0.0.1`。要直接对外暴露才设 `CLAWME_BIND`——先想清楚谁在
它前面挡着。

## 支持范围

这个项目还在快速演进，只对 `main` 分支的最新提交提供安全修复。
